#include <iostream>
#include "Admin.h"
#include "BankAccount.h"
#include "Client.h"
#include "System.h"
#include "DataBaseCollection.h"

int main()
{
    //DataBaseCollection collection;
    //std::string egn = "0146049340dsv";
    //a.deleteAnEmployee(egn);
    
    /*convertToChar(egn);*/
    system();

    //std::string randomEgn = collection.getEmployees()[0]->getEgn();
    //collection.addAClient(randomEgn);
    //collection.addANewBankAccount(randomEgn); 
    //collection.addANewBankCard(randomEgn);
    //collection.addANewBankCard(randomEgn);
    //collection.deleteAClient(randomEgn);
    //collection.closeABankAccount(randomEgn);
    //collection.closeABankCard(randomEgn);
    //collection.reportOfClients(randomEgn);
    //collection.reportAClient(randomEgn);

}