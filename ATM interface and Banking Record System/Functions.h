#pragma once
#include <iostream>
#include <vector>
#include <fstream>
#include <string>


std::string convertToString(const char*);
int countLines(const char*);
void copyNextWordfromAnArr(char*, char*);